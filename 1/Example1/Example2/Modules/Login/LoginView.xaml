<UserControl x:Class="Example2.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d">

    <StackPanel Margin="40" HorizontalAlignment="Center">
        
        <TextBox Margin="0,0,0,10" Width="200" 
                 Text="{Binding UserId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

        <PasswordBox Margin="0,0,0,10" Width="200"
                     PasswordChanged="PasswordBox_PasswordChanged" />

        <Button Content="로그인" Width="200"
                Command="{Binding LoginCommand}"
                IsEnabled="{Binding CanLogin}" />

        <TextBlock Text="{Binding StatusMessage}" FontSize="14" Margin="0,20,0,0"/>
    </StackPanel>
</UserControl>